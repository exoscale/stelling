# Stelling examples

This module contains a simple example of a Stelling-based project.
The codebase contains:

- An HTTP server
- A gRPC server
- A job

The goal is to provide a guideline on how to structure the code, load config
and produce a system. The code has been thoroughly commented to explain what
is going on at each point.

Detailed information on the capabilities of each module, can be found in the
module README and example tests.

The example also contains a simple `Earthfile`.

## Internal Exoscale projects

For bootstrapping internal Exoscale projects, see
[go-bootstrap](https://github.com/exoscale/go-bootstrap).
